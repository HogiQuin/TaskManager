<div class="container">
  <legend><%= @task.title %></legend>
  <p class="text-right"><strong>Date to deliver:</strong></p>
  <p class="text-right"><%= @task.date_to_deliver %></p>
  <article>
    <%= @task.description %>
  </article>
  <br><br>
  <div class="text-right">
    <%= link_to "Edit", edit_task_path(@task), class: "btn btn-warning align-center" %>
  </div>
</div>
